
<h2><%= @course.title %></h2>
  <%= @course.subject %><br>
  <%= @course.description %>

<h3>Add a new assignment</h3>
<%= form_for [@course, @assignment] do |f| %>
  <% if @assignment.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@assignment.errors.count, "error") %> prohibited this post from being saved:</h2>

    <ul>
    <% @assignment.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
